# Claude Code 项目入口

本项目唯一有效的开发规范、当前架构、业务边界和项目记忆位于根目录 `AGENTS.md`。

使用 Claude Code 开始任何工作前，必须完整阅读 `AGENTS.md`。如果本文件、历史对话、旧文档或代码注释与 `AGENTS.md` 冲突，以 `AGENTS.md` 和当前源码为准；仍无法判断时先向用户确认。

长期稳定的架构选择及其理由记录在 `docs/architecture-decisions.md`。该文档用于解释“为什么这样设计”，不替代 `AGENTS.md` 中的执行规则，也不作为当前 API 和功能完成状态的依据。
